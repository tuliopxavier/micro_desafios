let somar = function (numA, numB) {
  return numA + numB;
};

let subtrair = function (numA, numB) {
  return numA - numB;
};

let multiplicar = function (numA, numB) {
  return numA * numB;
};

let dividir = function (numA, numB) {
  return numA / numB;
};
